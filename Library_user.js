class Library_user {
  constructor(library_username, library_password){
    this.library_username = library_username,
    this.library_password = library_password
  }
}

exports.Library_user = Library_user;
